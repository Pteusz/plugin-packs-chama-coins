---

✓ **extensao-schema** concluída

**Executado:** Modificado `includes/class-activator.php` em três pontos:
1. `$sql1` em `create_tables()` — inseridas colunas `coins_amount BIGINT UNSIGNED NOT NULL DEFAULT 0` e `coins_platform VARCHAR(10) NOT NULL DEFAULT 'ps'` entre `dme_ids` e `adm_id`
2. Adicionado método `maybe_migrate()` com `SHOW COLUMNS` + `ALTER TABLE ADD COLUMN` condicional para cada campo
3. `activate()` chama `self::maybe_migrate()` imediatamente após `self::create_tables()`

**Verificação:** passou — os três critérios satisfeitos

**Próxima fase:** `extensao-logica`
