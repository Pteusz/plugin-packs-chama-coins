---

✓ **fix-store-display** concluída

**Executado:**
1. `formatCoins(n)` adicionada como helper em `public/store.js`
2. `updateBar()` usa `total_price || price`
3. `renderGrid()` usa `total_price || price`
4. Modal header usa `total_price || price`
5. Bloco `!found.length` verifica `coins_amount > 0` antes de exibir mensagem vazia
6. `buildCoinsCard(pack)` chamado após `found.forEach` quando `coins_amount > 0`
7. Função `buildCoinsCard()` definida antes de `openPackModal`
8. CSS `.cc-modal-coins-item`, `.cc-modal-coins-badge`, `.cc-modal-coins-icon` e `.cc-modal-coins-label` adicionado ao final de `assets/style.css`

**Verificação:** passou — todas as 8 condições da translation satisfeitas.

**Próxima fase:** `fix-session-desc`
